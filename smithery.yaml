logo: https://raw.githubusercontent.com/Yarmoluk/ckg-agentforce/main/assets/logo.png

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    additionalProperties: false
  commandFunction: |-
    (config) => ({
      command: "uvx",
      args: ["ckg-agentforce"],
      env: {}
    })
